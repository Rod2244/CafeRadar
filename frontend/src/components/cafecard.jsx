import { Heart, Star, Volume2, Wifi, Zap } from 'lucide-react';

export default function CafeCard({ cafe, isSaved, onSelect, onToggleSave }) {
	return <article className="cafe-card" onClick={() => onSelect(cafe)}><div className="card-image-wrap"><img src={cafe.image} alt={cafe.name} className="card-image" /><div className="image-shade" /><span className={`status-badge ${cafe.isOpen ? 'open' : 'closed'}`}>{cafe.isOpen ? 'Open Now' : 'Closed'}</span><span className="distance-badge">{cafe.distance}</span><button className={`save-button ${isSaved ? 'saved' : ''}`} onClick={(event) => onToggleSave(event, cafe.id)} aria-label={isSaved ? `Remove ${cafe.name} from saved` : `Save ${cafe.name}`}><Heart size={15} fill={isSaved ? 'currentColor' : 'none'} /></button><div className="card-title"><h3>{cafe.name}</h3><p>{cafe.address}</p></div></div><div className="card-content"><p className="tagline">{cafe.tagline}</p><div className="metrics"><Metric icon={<Wifi size={13} />} value={cafe.wifiSpeed} label="Speed" color="cyan" /><Metric icon={<Zap size={13} />} value={cafe.outlets} label="Outlets" color="amber" /><Metric icon={<Volume2 size={13} />} value={cafe.noiseLevel} label="Noise" color="blue" /></div><div className="card-footer"><span className="rating"><Star size={14} fill="currentColor" /> {cafe.rating} <small>({cafe.reviewCount})</small></span><div className="style-tags">{cafe.coffeeStyles.map((style) => <span key={style}>{style}</span>)}</div></div></div></article>;
}

function Metric({ icon, value, label, color }) { return <div className={`metric ${color}`}><span>{icon} {value}</span><small>{label}</small></div>; }
